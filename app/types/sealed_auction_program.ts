/**
 * Placeholder until `anchor build` generates `../target/types/sealed_auction_program.ts`.
 * Copy the generated file here or adjust imports after building the program.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type SealedAuctionProgram = any;
